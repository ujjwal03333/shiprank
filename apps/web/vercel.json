{
  "buildCommand": "cd ../.. && pnpm turbo run build --filter=@shiprank/web",
  "outputDirectory": ".next",
  "installCommand": "cd ../.. && pnpm install",
  "crons": [
    {
      "path": "/api/cron/rescan",
      "schedule": "0 * * * *"
    }
  ]
}
